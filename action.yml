name: 'gsheet.action'
description: 'This action helps in CRUD operations on a google sheet.'
author: 'jroehl <mail@johannroehl.de>'
branding:
  icon: book
  color: green
inputs:
  startRow:
    description: 'Where to start looking'
    required: true
outputs:
  lastRow:
    description: 'The last row'
runs:
  using: 'node12'
  main: 'lib/main.js'
